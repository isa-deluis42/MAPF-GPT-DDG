# Requirements for the finetuning / congestion-classification workflow.
# This stays lighter than docker/requirements.txt but includes everything
# needed by the scripts in finetuning/.

numpy>=1.21.5,<2
torch>=2.0
pyarrow>=14.0.0
tqdm>=4.65.0
pydantic>=1.8.2,<2
